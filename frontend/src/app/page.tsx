"use client"; // This marks the component as a Client Component

import AccountDashboard from '../components/AccountDashboard'; // Import your main dashboard component

// This is your root page component that renders the main application dashboard.
export default function Home() {
    return <AccountDashboard />;
}
