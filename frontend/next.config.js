/** @type {import('next').NextConfig} */
const nextConfig = {
    // Add any Next.js specific configurations here
    // For example, if you need to set up image domains, redirects, etc.
    // images: {
    //   domains: ['example.com'],
    // },
};

module.exports = nextConfig;
